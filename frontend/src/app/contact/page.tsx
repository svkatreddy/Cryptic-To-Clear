import PageShell from "@/components/PageShell";

export const metadata = { title: "Contact — Cryptic to Clear" };

export default function ContactPage() {
  return (
    <PageShell
      eyebrow="get in touch"
      title="Talk to the Cryptic to Clear team"
      description="A contact form and support details are coming in the next phase."
    />
  );
}
